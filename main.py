# could run a loop with importlib to import all files from datasets
from pysql import app
from tables import *

# write the main logic of import and from tables/views import *. possibly don't need app from pysql.
# or don't need flask for main

# if __name__ == "__main__":
#     app.run(debug=True)
